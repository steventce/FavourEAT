import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  settingsBtn: {
    borderBottomWidth: 0,
    marginBottom: 10,
    marginTop: 10,
    padding: 0
  },
  label: {
    fontWeight: 'bold'
  }
});

export default styles;
