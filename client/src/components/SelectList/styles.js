import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
  selectItem: {
    flex: 1,
    flexDirection: 'row'
  },
  label: {
    marginLeft: 10
  }
});

export default styles;
